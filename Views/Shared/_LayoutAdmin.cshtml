<!DOCTYPE html>
<html lang="vi">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>@ViewData["Title"] - TechSkill Academy Admin</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.5/font/bootstrap-icons.css" rel="stylesheet">
    <link href="~/css/admin.css" rel="stylesheet" />
    @RenderSection("Styles", required: false)
</head>
<body>

    <div class="d-flex" id="wrapper">

        <!-- Sidebar -->
        <nav id="sidebar">
            <div class="sidebar-header text-center py-4">
                <h4 class="text-white">Admin Panel</h4>
            </div>
            <ul class="list-unstyled components px-3">
                <li class="sidebar-item mb-2">
                    <a asp-controller="Admin" asp-action="Dashboard" class="sidebar-link">Dashboard</a>
                </li>
                <li class="sidebar-item mb-2">
                    <a asp-controller="Admin" asp-action="ManageUsers" class="sidebar-link">Quản lý người dùng</a>
                </li>
                <li class="sidebar-item mb-2">
                    <a asp-controller="Admin" asp-action="ManageCourses" class="sidebar-link">Quản lý khóa học</a>
                </li>
                <li class="sidebar-item mb-2">
                    <a asp-controller="Admin" asp-action="Reports" class="sidebar-link">Báo cáo</a>
                </li>
                <li class="sidebar-item mt-4">
                    <a asp-controller="Auth" asp-action="Logout" class="sidebar-link logout-link">Đăng xuất</a>
                </li>
            </ul>
        </nav>

        <!-- Page Content -->
        <div id="page-content-wrapper" class="w-100">
            <nav class="navbar navbar-expand-lg navbar-light bg-light border-bottom">
                <div class="container-fluid">
                    <button class="btn btn-primary" id="menu-toggle">☰</button>
                    <span class="ms-3 fw-bold">Xin chào, @User.Identity.Name</span>
                </div>
            </nav>

            <div class="container-fluid mt-4">
                @RenderBody()
            </div>
        </div>

    </div>

    <!-- Bootstrap Bundle -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
    <script src="~/js/admin.js" asp-append-version="true"></script>
    @RenderSection("Scripts", required: false)
</body>
</html>
